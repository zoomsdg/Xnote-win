<Window x:Class="XNote.App.ImportTargetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="导入到标签页" Height="220" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="把导入的纪事放到哪个标签页？" Margin="0,0,0,10"/>

        <RadioButton x:Name="NewRadio" Grid.Row="1" Content="新建标签页" GroupName="Target"
                     IsChecked="True" VerticalContentAlignment="Center"/>
        <TextBox x:Name="NewName" Grid.Row="2" Padding="4" Height="28" Margin="22,4,0,8"
                 IsEnabled="{Binding IsChecked, ElementName=NewRadio}"/>

        <RadioButton x:Name="ExistingRadio" Grid.Row="3" Content="导入到现有标签页" GroupName="Target"
                     VerticalContentAlignment="Center"/>
        <ComboBox x:Name="ExistingBox" Grid.Row="4" Height="28" Margin="22,4,0,0"
                  VerticalAlignment="Top" VerticalContentAlignment="Center"
                  DisplayMemberPath="Name" SelectedValuePath="Id"
                  IsEnabled="{Binding IsChecked, ElementName=ExistingRadio}"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="确定" IsDefault="True" Click="Ok_Click"/>
            <Button Content="取消" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>

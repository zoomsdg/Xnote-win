<Application x:Class="XNote.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:XNote.App"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- 主色调 -->
        <SolidColorBrush x:Key="Accent" Color="#FF3B6EA5"/>
        <SolidColorBrush x:Key="ChipBg" Color="#FFE8EEF6"/>
        <SolidColorBrush x:Key="Muted" Color="#FF7A7A7A"/>

        <Style TargetType="Button">
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="MinWidth" Value="64"/>
        </Style>

        <Style x:Key="Chip" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource ChipBg}"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="6,1"/>
        </Style>
    </Application.Resources>
</Application>

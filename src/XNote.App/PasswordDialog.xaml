<Window x:Class="XNote.App.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="密码" Height="200" Width="380"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="PromptText" Grid.Row="0" Text="请输入密码" Margin="0,0,0,8"/>
        <PasswordBox x:Name="Pwd" Grid.Row="1" Padding="4" Height="28"/>
        <PasswordBox x:Name="Pwd2" Grid.Row="2" Padding="4" Height="28" Margin="0,8,0,0" Visibility="Collapsed"/>
        <TextBlock x:Name="Err" Grid.Row="3" Foreground="Red" Margin="0,8,0,0" TextWrapping="Wrap"/>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="确定" IsDefault="True" Click="Ok_Click"/>
            <Button Content="取消" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
